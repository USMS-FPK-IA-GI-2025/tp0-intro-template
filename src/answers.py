# === À COMPLÉTER PAR L'ÉTUDIANT.E ===
# Indique ton identifiant GitHub (chaîne de caractères, ex: "yelallioui")
STUDENT_GITHUB = ""  # TODO: renseigner votre identifiant GitHub

def add(a, b):
    """Retourne la somme a + b (types numériques)."""
    # TODO: implémenter
    raise NotImplementedError

# --- NumPy ---
import numpy as np
# TODO: créer un array NumPy [1, 2, 3] nommé `arr`
# TODO: créer un scalaire `arr_mean` qui vaut la moyenne de `arr`
arr = None
arr_mean = None

def normalize(x: np.ndarray) -> np.ndarray:
    """Normalisation min-max : (x - x.min()) / (x.max() - x.min()).
    Si tous les éléments sont égaux, retourner un array de zéros de même forme.
    """
    # TODO: implémenter
    raise NotImplementedError
